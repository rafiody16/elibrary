<?php

use Illuminate\Foundation\Inspiring;
use Illuminate\Support\Facades\Artisan;
use Illuminate\Support\Facades\Schedule;

// Artisan::command('inspire', function () {
//     $this->comment(Inspiring::quote());
// })->purpose('Display an inspiring quote');

// Artisan::command('loans:calculate-delay', function () {
//     $this->call(DelayCalculation::class);
// })->purpose('Calculate overdue fines for all borrowed loans daily');

// return function (Schedule $schedule) {
//     // Schedule the command to run daily at midnight
//     $schedule->command('loans:calculate-delay')->dailyAt('00:01');
// };